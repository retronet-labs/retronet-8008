# Testdata

Questa cartella conterra' ROM e dati di test generati o curati per verificare
l'emulatore Intel 8008.

Per ora non contiene binari: le ROM arriveranno dopo l'introduzione del decoder,
della memoria e degli helper opcode.
